'use strict';

(function () {
  'use strict';

  // Set up variables we'll need
  var foo = 1;

  // Set up a loop and output some messages
  while (foo < 10) {
    console.log('The count is at ' + foo++);
  }
})();